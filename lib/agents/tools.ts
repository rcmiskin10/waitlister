import type { AgentType } from '@/types/agents'

// Tool definitions placeholder
// These will be expanded with actual implementations

export function getAgentTools(agentType: AgentType) {
  // Return empty for now - tools will be added in future phases
  // The agent can still function without tools using just the LLM
  return {}
}
